<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.woongs.mapper.CommonCodeMapper">
	<select id="selectCommonCode" resultType="com.woongs.vo.CommonVO">
		SELECT
			cm_key, cm_value
		FROM
			tb_common
	</select>
</mapper>